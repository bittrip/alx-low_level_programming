#include <stdio.h>

/**
 * main - Program's entry point.
 * Description: Prints a text to screen using printf() function.
 * Return: 0 if successful.
 */
int main(void)
{
char str[] = "with proper grammar, but the outcome is a piece of art,\n";
printf("%s", str);
return (0);
}
